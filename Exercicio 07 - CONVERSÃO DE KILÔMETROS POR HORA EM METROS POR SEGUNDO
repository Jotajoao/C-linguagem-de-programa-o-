#include <stdio.h>

int main(void) {
  int k;
  float m;

  printf("Informe a velocidade em km/h:\n");
  scanf("%d", &k);

  m = k / 3.6;

  printf("A velocidade em km/h convertida em m/s é igual a %f", m);
  return 0;
}
