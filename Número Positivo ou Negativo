#include <stdio.h>

int main(void) {
  int num;

  printf ("Informe um número:\n");
  scanf ("%d", &num);
  if (num >= 0){
    printf ("O número é POSITIVO");
  }else {
    printf("O número é NEGATIVO");
  }

  return 0;
}
